# --- Active profiles ---
spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:h2}
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false

# --- DB timeout --
javax:
  persistence:
    query:
      timeout: 10000